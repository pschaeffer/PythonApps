# This Python program was created to debug some 
# issues with the System Path (sys.path) in Python.

import sys

print('\n'.join(sys.path))
print('Blank line') 
print(sys.prefix)
print(sys.exec_prefix)